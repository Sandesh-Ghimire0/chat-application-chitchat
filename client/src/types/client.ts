export interface User {
    id: string;
    username: string;
    image: string;
    token:string
}

export interface Friend {
    id: string;
    username: string;
    isActive: boolean;
    image: string;
}

